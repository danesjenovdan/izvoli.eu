name: Automatic PRs
on:
  push:
    branches:
      - "izvoli-si-simple"
jobs:
  pull_requests:
    name: Automatic PRs
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v6

      - name: Pull request to izvoli-si-simple-k8s
        uses: repo-sync/pull-request@v2
        with:
          destination_branch: "izvoli-si-simple-k8s"
          github_token: ${{ secrets.GITHUB_TOKEN }}
          pr_title: "Pulling ${{ github.ref }} into izvoli-si-simple-k8s"
